import { css } from "../../stitches.config";

export const correctImg = css({
    display: "flex",
    justifyContent: "space-evenly",
    padding: "30px"
})

export const quizContainer = css({
    display: "flex",
    gap: "10px",
    flexDirection: "column"
})